# Пошаговая инструкция: Установка и сборка RadioMaster Configurator

## Шаг 1: Установка Node.js

Node.js необходим для работы с проектом и сборки приложения.

### Скачивание и установка

1. **Перейдите на официальный сайт Node.js:**
   - Откройте браузер и перейдите по ссылке: https://nodejs.org/
   
2. **Скачайте LTS версию:**
   - Нажмите на кнопку "LTS" (рекомендуемая версия для большинства пользователей)
   - Это скачает установщик для Windows (файл .msi)

3. **Запустите установщик:**
   - Дважды кликните на скачанный файл
   - Следуйте инструкциям мастера установки
   - **Важно:** Убедитесь, что установлена галочка "Automatically install the necessary tools" (Автоматически устанавливать необходимые инструменты)
   - Нажмите "Next" → "Next" → "Install"

4. **Проверьте установку:**
   - Откройте **новое** окно PowerShell или командной строки
   - Выполните команду:
   ```powershell
   node --version
   ```
   - Вы должны увидеть номер версии, например: `v20.11.0`
   
   - Также проверьте npm:
   ```powershell
   npm --version
   ```
   - Вы должны увидеть номер версии, например: `10.2.4`

> **Примечание:** Если команды не работают, перезагрузите компьютер и попробуйте снова.

---

## Шаг 2: Установка зависимостей проекта

После установки Node.js:

1. **Откройте PowerShell или командную строку**

2. **Перейдите в директорию проекта:**
   ```powershell
   cd D:\RM-Connect
   ```

3. **Установите зависимости:**
   ```powershell
   npm install
   ```
   
   Этот процесс может занять 2-5 минут. Вы увидите прогресс установки пакетов.
   
   После завершения должна появиться директория `node_modules` с установленными пакетами.

> **Что устанавливается:**
> - `electron` (~200 MB) - фреймворк для создания десктопных приложений
> - `electron-builder` (~50 MB) - инструмент для сборки .exe файла

---

## Шаг 3: Тестирование приложения в режиме разработки

Перед сборкой .exe рекомендуется протестировать приложение:

```powershell
npm start
```

**Что должно произойти:**
- Откроется окно приложения RadioMaster Configurator
- Интерфейс должен отображаться корректно
- Шрифты должны загружаться (проверьте, что текст выглядит нормально)
- Меню браузера должно быть скрыто

**Тестирование Web Serial API:**
1. Подключите устройство RadioMaster к компьютеру
2. В приложении попробуйте подключиться к устройству
3. Проверьте, что приложение может обнаружить serial порт

Для закрытия приложения нажмите `Alt+F4` или закройте окно.

---

## Шаг 4: Сборка .exe файла

Когда вы убедились, что приложение работает корректно:

```powershell
npm run build
```

**Процесс сборки:**
- Может занять 5-10 минут при первой сборке
- electron-builder загрузит необходимые компоненты (~100 MB)
- Будет создан установщик и портативная версия

**Результат:**
- Сборка завершится без ошибок
- В директории `D:\RM-Connect\dist\` появятся следующие файлы:
  - `RadioMaster Configurator Setup X.X.X.exe` - установщик
  - Папка `win-unpacked\` - портативная версия

**Размеры файлов (примерно):**
- Установщик: ~70-90 MB
- Портативная версия: ~140-160 MB

---

## Шаг 5: Тестирование собранного .exe

1. **Найдите установщик:**
   ```
   D:\RM-Connect\dist\RadioMaster Configurator Setup X.X.X.exe
   ```

2. **Запустите установщик:**
   - Дважды кликните на файл
   - Выберите папку установки (или оставьте по умолчанию)
   - Нажмите "Install"

3. **Запустите установленное приложение:**
   - Ярлык появится на рабочем столе или в меню "Пуск"
   - Запустите приложение

4. **Проверьте работу:**
   - Приложение должно работать без установленного Node.js
   - Отключите интернет и убедитесь, что приложение работает оффлайн
   - Подключите устройство RadioMaster и проверьте функции конфигурации

---

## Шаг 6: Подготовка к публикации на GitHub

Если вы хотите опубликовать проект на GitHub:

1. **Убедитесь, что Git установлен:**
   ```powershell
   git --version
   ```
   
   Если Git не установлен, скачайте с https://git-scm.com/

2. **Инициализируйте репозиторий:**
   ```powershell
   cd D:\RM-Connect
   git init
   ```

3. **Добавьте файлы:**
   ```powershell
   git add .
   git commit -m "Initial commit: RadioMaster Configurator Electron App"
   ```

4. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/
   - Нажмите "New repository"
   - Назовите репозиторий (например, `RM-Connect`)
   - **НЕ** инициализируйте с README (у нас уже есть)

5. **Загрузите код на GitHub:**
   ```powershell
   git remote add origin https://github.com/ваш-username/RM-Connect.git
   git branch -M main
   git push -u origin main
   ```

6. **Загрузите .exe в Releases:**
   - Перейдите в ваш репозиторий на GitHub
   - Нажмите "Releases" → "Create a new release"
   - Заполните информацию о версии (например, v1.0.0)
   - Прикрепите `.exe` файл из `dist/`
   - Опубликуйте релиз

---

## Команды для быстрого доступа

```powershell
# Установка зависимостей (выполняется один раз)
npm install

# Запуск в режиме разработки
npm start

# Сборка установщика .exe
npm run build

# Сборка портативной версии (без установщика)
npm run build:dir
```

---

## Решение возможных проблем

### Ошибка: "npm is not recognized"

**Причина:** Node.js не установлен или не добавлен в PATH

**Решение:**
1. Убедитесь, что Node.js установлен
2. Перезагрузите компьютер
3. Откройте НОВОЕ окно PowerShell

### Ошибка при npm install

**Причина:** Проблемы с сетью или правами доступа

**Решение:**
```powershell
# Очистить кеш npm
npm cache clean --force

# Попробовать установить снова
npm install
```

### Ошибка при сборке electron-builder

**Причина:** Недостаточно места на диске или антивирус блокирует

**Решение:**
1. Освободите минимум 5 GB на диске
2. Временно отключите антивирус
3. Попробуйте собрать снова:
   ```powershell
   npm run build
   ```

### Устройство не обнаруживается

**Причина:** Отсутствуют драйверы USB-Serial

**Решение:**
1. Установите драйверы для вашего устройства RadioMaster
2. Проверьте в "Диспетчере устройств", что устройство определяется
3. Перезапустите приложение

---

## Итоговая структура проекта

```
D:\RM-Connect\
├── node_modules/         # Установленные зависимости (создается при npm install)
├── dist/                 # Результаты сборки (создается при npm run build)
│   ├── RadioMaster Configurator Setup X.X.X.exe
│   └── win-unpacked/     # Портативная версия
├── fonts/                # Локальные шрифты
│   ├── Roboto-Regular.ttf
│   └── Roboto-Bold.ttf
├── .gitignore            # Исключения для Git
├── Connection.html       # Оригинальный файл (для справки)
├── index.html            # Основной интерфейс (модифицированный)
├── main.js               # Главный процесс Electron
├── package.json          # Конфигурация проекта
├── preload.js            # Preload скрипт
└── README.md             # Документация проекта
```

---

**Готово!** Ваше приложение RadioMaster Configurator готово к использованию и распространению! 🎉
