# Краткая инструкция: Публикация и сборка проекта

## ⚠️ ВАЖНО: Сначала установите Node.js!

Проект готов к публикации на GitHub, но для сборки .exe файла **требуется Node.js**.

### Установка Node.js (обязательный шаг)

1. **Скачайте Node.js:**
   - Перейдите на https://nodejs.org/
   - Нажмите на кнопку **"LTS"** (рекомендуемая версия)
   - Скачайте установщик для Windows

2. **Установите Node.js:**
   - Запустите скачанный .msi файл
   - Следуйте инструкциям (все галочки оставьте по умолчанию)
   - Нажмите "Next" → "Next" → "Install"
   - **Важно:** После установки ПЕРЕЗАГРУЗИТЕ компьютер!

3. **Проверьте установку (после перезагрузки):**
   ```powershell
   node --version
   npm --version
   ```
   Должны появиться номера версий.

---

## 🚀 Сборка .exe файла

После установки Node.js выполните следующие команды:

### 1. Установите зависимости (один раз)

```powershell
cd D:\RM-Connect
npm install
```

⏱️ Это займет 3-5 минут. Загрузится ~250-300 MB пакетов.

### 2. (Опционально) Протестируйте приложение

```powershell
npm start
```

Откроется окно приложения. Проверьте интерфейс и функциональность.
Закройте окно, когда убедитесь, что все работает.

### 3. Соберите .exe файл

```powershell
npm run build
```

⏱️ Это займет 5-10 минут при первой сборке.

**Результат:** В директории `D:\RM-Connect\dist\` появятся:
- `RadioMaster Configurator Setup 1.0.0.exe` - установщик (~70-90 MB)
- Папка `win-unpacked\` - портативная версия

---

## 📤 Публикация на GitHub

Git репозиторий уже инициализирован и готов к публикации!

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"New"** (зеленая кнопка)
4. Введите имя репозитория: `RM-Connect`
5. Добавьте описание: `RadioMaster Configurator - Electron app with Web Serial API support`
6. Выберите **Public** (публичный) или **Private** (приватный)
7. **НЕ** ставьте галочки:
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license
8. Нажмите **"Create repository"**

### 2. Настройте свой email и имя (если не настроено глобально)

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"
```

> **Примечание:** Используйте тот же email, что и на GitHub!

### 3. Загрузите код на GitHub

GitHub покажет вам команды. Выполните следующие:

```powershell
cd D:\RM-Connect
git remote add origin https://github.com/ваш-username/RM-Connect.git
git branch -M main
git push -u origin main
```

> **Важно:** Замените `ваш-username` на ваш реальный username на GitHub!

При первой отправке может потребоваться авторизация через браузер или Personal Access Token.

### 4. Проверьте результат

Обновите страницу репозитория на GitHub - вы должны увидеть все файлы проекта!

---

## 🎁 Создание Release с .exe файлом

После сборки .exe можно создать Release для удобного скачивания:

### 1. Перейдите в раздел Releases

1. Откройте ваш репозиторий на GitHub
2. Нажмите **"Releases"** (справа на странице)
3. Нажмите **"Create a new release"**

### 2. Заполните информацию

- **Choose a tag:** `v1.0.0` (создайте новый тег)
- **Release title:** `RadioMaster Configurator v1.0.0`
- **Describe this release:**
  ```
  🎉 Первый релиз RadioMaster Configurator!
  
  ## Особенности
  - ✅ Поддержка Web Serial API
  - ✅ Полностью оффлайн работа
  - ✅ Автоматический доступ к последовательным портам
  - ✅ Чистый интерфейс без меню браузера
  
  ## Установка
  Скачайте и запустите файл `RadioMaster.Configurator.Setup.1.0.0.exe`
  
  ## Системные требования
  - Windows 10/11 (64-bit)
  - ~150 MB свободного места
  ```

### 3. Прикрепите .exe файл

1. Нажмите **"Attach binaries"** внизу
2. Выберите файл: `D:\RM-Connect\dist\RadioMaster Configurator Setup 1.0.0.exe`
3. Дождитесь загрузки файла

### 4. Опубликуйте Release

Нажмите **"Publish release"**

**Готово!** Теперь любой может скачать ваш .exe файл по прямой ссылке!

---

## 📋 Краткая последовательность команд

```powershell
# 1. Установка Node.js (через установщик с сайта nodejs.org)
# 2. Перезагрузка компьютера
# 3. Проверка установки
node --version
npm --version

# 4. Установка зависимостей
cd D:\RM-Connect
npm install

# 5. Сборка .exe
npm run build

# 6. Настройка Git (если нужно)
git config --global user.name "Ваше Имя"
git config --global user.email "your-email@example.com"

# 7. Публикация на GitHub
git remote add origin https://github.com/ваш-username/RM-Connect.git
git branch -M main
git push -u origin main
```

---

## 🎯 Текущий статус проекта

✅ **Готово:**
- Git репозиторий инициализирован
- Создан первый коммит со всеми файлами (10 файлов)
- Все файлы готовы к публикации

⏳ **Требуется выполнить:**
1. Установить Node.js
2. Установить npm зависимости
3. Собрать .exe файл
4. Создать репозиторий на GitHub и загрузить код
5. Создать Release с .exe файлом

---

## ❓ Возможные проблемы

### Ошибка при git push

Если появляется ошибка авторизации:

**Решение:**
1. Создайте Personal Access Token на GitHub:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите scope: `repo`
   - Скопируйте токен

2. Используйте токен вместо пароля при push

### npm install не работает

**Решение:**
1. Убедитесь, что Node.js установлен: `node --version`
2. Перезагрузили ли вы компьютер после установки?
3. Попробуйте закрыть и открыть новое окно PowerShell

### electron-builder ошибка при сборке

**Решение:**
1. Освободите место на диске (минимум 5 GB)
2. Временно отключите антивирус
3. Попробуйте снова: `npm run build`

---

**Удачи с публикацией проекта! 🚀**
